
#function to count no of zeros in a digit
def count_zeros(digit):
    a=1
    dig=0
    while(True):
        a=a*10
        if(digit%a==0):
            dig=dig+1
        else: 
            break
    return dig

count_zeros(2000000000)

#9
